environment: {{ environment }}
{% if experimentName is defined %}
experimentName: {{ experimentName }}
{% endif %}
feature_path: "{{ feature_path | default('s3://thetradedesk-mlplatform-us-east-1/configdata/prod/audience/schema/RSMV2/v=1/' ~ run_date.strftime(full_version_date_format) ~ '/features.json') }}"
runDate: "{{ run_date.strftime(run_date_format) }}" # job running logical date

audienceJarBranch: "{{ audienceJarBranch | default('master') }}"
audienceJarVersion: "{{ audienceJarVersion | default('latest') }}"
